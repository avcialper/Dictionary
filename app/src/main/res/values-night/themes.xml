<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Dictionary" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
        <item name="search_background">@color/border_color</item>
        <item name="app_background">@color/black</item>
        <item name="card_background">@color/border_color</item>
        <item name="text_color">@color/white</item>
    </style>
</resources>